{
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Persistent memory across Claude sessions for context retention"
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-fetch"],
      "description": "HTTP fetch capabilities for web resource access and API interactions"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "description": "Enhanced reasoning capabilities for complex multi-step decisions and planning"
    }
  },
  "_comment": "MCP servers configuration for NABIP Association Management platform. GitHub MCP is configured globally. Additional servers (Notion, Monday, Brave Search) can be enabled as needed.",
  "_setup": {
    "memory": "No configuration required - automatically manages context persistence",
    "fetch": "No configuration required - provides HTTP/HTTPS fetch capabilities",
    "sequential-thinking": "No configuration required - enhances Claude's reasoning process"
  }
}